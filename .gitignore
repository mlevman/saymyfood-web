# Task files. Never committed: an identically named prompt.md lives in the
# NutrientAI app repository, and on 2026-09-09 a task was pasted into the wrong
# project and run with stale text. Different file names are the barrier.
prompt-web.md
prompt.md

# The unattended-run launcher and everything it produces. The launcher holds the
# owner's local paths and is not part of the site; night_logs/ is where it puts the
# archived journals and the per-attempt transcripts.
run_night_web.ps1
RUN_LOG.md
night_logs/
attempt_*.log

# Tooling and OS noise.
.claude/
.DS_Store
Thumbs.db
